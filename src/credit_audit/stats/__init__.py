"""Phase 7 statistics.

Nothing in this package invents a hypothesis. :mod:`credit_audit.stats.families` loads the
preregistration and refuses to score an undeclared family; every other module here is a
pure function over counts that the checks layer already produced.
"""
