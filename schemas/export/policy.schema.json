{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "credit-audit/policy@1",
  "title": "Underwriting policy snapshot",
  "description": "Machine mirror of policy.yaml with hashes, so the site's threshold table can never drift from what the run actually used. Section anchors let the pair viewer deep-link a threshold to its prose in policy.md.",
  "type": "object",
  "required": ["schema", "version", "md_sha256", "yaml_sha256", "product", "thresholds", "required_tools_before_decision", "required_tools_before_code", "prohibited_tools", "prohibited_factors", "min_stated_reasons", "max_stated_reasons"],
  "properties": {
    "schema": { "const": "credit-audit/policy@1" },
    "version": { "type": "string" },
    "md_sha256": { "type": "string" },
    "yaml_sha256": { "type": "string" },
    "product": {
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "secured": { "type": "boolean" },
        "synthetic": {
          "const": true,
          "description": "Always true. This is a synthetic lender policy against synthetic applicants; the project never claims to replicate any real lender's underwriting."
        }
      }
    },
    "thresholds": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "field", "operator", "value", "reason_code"],
        "properties": {
          "id": { "type": "string" },
          "field": { "type": "string" },
          "operator": { "enum": ["<", "<=", ">", ">=", "==", "!="] },
          "value": { "type": ["string", "number", "boolean"] },
          "reason_code": { "type": "string" },
          "anchor": { "type": ["string", "null"] },
          "description": { "type": "string" }
        }
      }
    },
    "required_tools_before_decision": { "type": "array", "items": { "type": "string" } },
    "required_tools_before_code": {
      "type": "object",
      "additionalProperties": {
        "type": "array",
        "items": { "type": "string" }
      }
    },
    "prohibited_tools": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Calling one of these is a policy violation regardless of the decision reached -- the tau2-bench pattern: task completed plus policy violated equals FAIL."
    },
    "prohibited_factors": { "type": "array", "items": { "type": "string" } },
    "min_stated_reasons": {
      "type": "integer",
      "minimum": 0,
      "description": "Synthetic policy minimum for an adverse decision; not a statutory number."
    },
    "max_stated_reasons": {
      "type": "integer",
      "minimum": 1,
      "description": "Synthetic policy maximum, not a statutory cap. The official Regulation B interpretation does not mandate a number and says only that disclosure of more than four reasons is unlikely to be helpful. The decision tool allows more so an excess remains observable."
    }
  }
}
